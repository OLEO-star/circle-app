# 引き継ぎ — 学部診断 circle-app（2026-06-13）

別ターミナル／別セッションで作業を続けるための引き継ぎ。**これ1枚＋下記の正本を読めば続きから動ける。**

---

## 0. まず読む（順番）
1. `~/home/my-company/.company/ORG-MAP.md` — 組織の地図
2. `~/home/my-company/.company/secretary/STATUS.md` の ring-map 欄 — 全体の現状
3. `~/home/my-company/.company/secretary/notes/circle-app-overview.md` — **デプロイ・URL・Sheets の正本**
4. このファイル

## 1. いまの状態（1行）
質問追加（新3軸 PURE/BIO/PROC・6問）＋判定式v2 を実装・検証・**ローカルコミット済み。GitHub未push・本番未反映**。リングは現状維持で確定。

## 2. 完了していること
- **質問追加**: PURE(19)/BIO(20)/PROC(21) の3軸・6問（P1/P2/P3/Bi1/Bi2/Pr1）を sciences＋mixed に追加。humanities 53問は不変。sciences 58問・mixed 66問。
- **判定式v2**: コサイン → wL2_var(γ0.7)＋必須軸ゲート(0.45/G2/GRAD除外)。表示スコア exp(−0.35·実効距離^1.5)。
- **検証**: 多視点サブエージェント監査（設計・実装ともPASS）。復元率シミュ（合成データ）で chem4 ~87%・info2 ~96%・偽物棄却100%・humanities劣化なし。`analysis/2026-06-12-*.md`。
- **E2E**: 実ブラウザで58問回答→結果描画OK（新6問表示・top1化学科98）。
- **リングデザイン**: 第1回4案を提示したが、**かずき決定＝現状維持（案A）。B/C/D は不採用・保留**（`analysis/ring-design-preferences.md`）。
- **縦スクロールdemo**: `src/app/result-v/`（`/result-v?demo=sciences`）。採否未定・本番未統合。

## 3. ★デプロイ（最重要・未実施）
**現状: ローカルコミットのみ。origin/main より5コミット先行（`bdcbbd7`→`94c1b4a`）。push していない＝本番 ring-map.com は旧版のまま。**

デプロイ機構（`circle-app-overview.md` が正本）:
| 項目 | 値 |
|---|---|
| 本番URL | https://ring-map.com/ （CF Pages バックエンド = circle-app-17o.pages.dev） |
| トリガー | **GitHub `OLEO-star/circle-app` の main に push → Cloudflare Pages が自動ビルド→自動公開** |
| Build command | `npm run build`（`output:"export"` で `out/` 生成） |
| Build output | `out` ／ Framework preset = **None**（Next.js選択は失敗するので不可） |

→ **push がそのまま本番公開になる。** 公開は L2（オーナーGO必須）。GOが出るまで push しない。

### デプロイ前に必須の残作業（Sheets/Apps Script）
**いま push すると新3軸データが記録されない／既存シートと列がズレる。**

✅ **コード側は完了・コミット済み（`5654945`、未push）**:
- [x] `code.gs`: `parseAxisScores` を 19→22、軸展開（`axisValues[19][20][21]`）、`DIAGNOSES_HEADER`/`SCHOOL_STUDENTS_HEADER` に日本語3列（純粋志向/生体・生命現象/量産・プロセス）追加
- [x] 既存シートの列ズレ対策＝**移行関数 `migrateAddNewAxisColumns()` を新設**（身体運動の直後に空3列を冪等挿入。旧行は空欄のまま）。手順は `scripts/apps-script/README.md`
- [x] `npm run build` ローカル通過（型OK・`out/`生成）

⬜ **オーナー手作業（Google側・push前に）**:
- [ ] Apps Script エディタに更新版 `code.gs` を貼り直す → 関数 `migrateAddNewAxisColumns` を実行（マスタ diagnoses ＋ 学校別 students 全部に3列挿入。ログ確認）
- [ ] Apps Script を「**新バージョン**」で再デプロイ（URL不変・README手順）

⬜ **要オーナー判断（公開コピーの整合・対外文書なのでレビュー対象）**:
- [ ] サイト本文の「**19軸**」「**60問**」表記の扱い（`layout.tsx`/`opengraph-image.alt.txt`/`twitter-image.alt.txt`/`privacy/page.tsx`/`teachers/page.tsx`）。総数は22軸だが **humanities は実質19軸（新3軸はゼロ化）／問数も版で53・58・66** のため「22軸」と言い切れない。コピーをどう書くかは要判断（今回コードは触っていない）

### デプロイ手順（GOが出たら）
1. 上記 Sheets/Apps Script 対応を先に完了
2. `npm run build` がローカルで通ることを確認（型・ビルド）
3. `git push origin main` → CF Pages 自動ビルドを待つ → ring-map.com で新6問・v2 を目視確認
4. デモ確認: `/result?demo=sciences`／`/result?demo=mixed`

## 4. 未決・オーナー判断待ち
- 縦スクロール（`/result-v`）を本番採用するか
- デプロイGO（上記 Sheets 対応とセットで）
- 文系2〜3問追加の話（記録になし。記憶の出どころ未確認＝今回は見送り済み）

## 5. 主要ファイル
| 役割 | パス |
|---|---|
| 質問プール・出題順 | `src/lib/questions.ts` |
| 学科×22軸ベクトル | `src/lib/departments.ts` |
| 判定式v2 | `src/lib/scoring.ts` |
| リング描画（現状維持） | `src/components/Ring.tsx` |
| 結果画面（本番） | `src/app/result/page.tsx` |
| 縦スクロールdemo | `src/app/result-v/page.tsx` |
| 設計・監査・検証記録 | `analysis/2026-06-12-*.md`, `analysis/2026-06-13-*.md` |
| デザイン好みの蓄積 | `analysis/ring-design-preferences.md` |
| Apps Script | `scripts/apps-script/code.gs` + `README.md` |
| 退避点（コミット） | `bdcbbd7`（実装前チェックポイント） |
